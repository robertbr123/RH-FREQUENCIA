{
  "name": "rhf-sistema-frequencia",
  "version": "1.0.0",
  "description": "Sistema de RH para Registro de Frequência de Funcionários",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client\"",
    "server:dev": "nodemon api/index.js",
    "server": "node api/index.js",
    "client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "install-all": "npm install && cd api && npm install && cd ../client && npm install"
  },
  "keywords": ["rh", "frequencia", "ponto", "funcionarios"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1"
  }
}
