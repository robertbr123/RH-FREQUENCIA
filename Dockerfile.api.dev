# Dockerfile para API/Backend - Desenvolvimento com hot-reload
FROM node:18-alpine

# Instalar dependências do sistema
RUN apk add --no-cache postgresql-client

# Instalar nodemon globalmente para hot-reload
RUN npm install -g nodemon

# Definir diretório de trabalho
WORKDIR /app

# Copiar package.json
COPY api/package.json ./

# Instalar dependências (incluindo dev)
RUN npm install

# Expor porta da API
EXPOSE 3001

# Comando para desenvolvimento com nodemon
CMD ["nodemon", "index.js"]
